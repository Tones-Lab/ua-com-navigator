{
  "name": "com-management",
  "version": "0.1.0",
  "description": "FCOM Curation & Management Interface - Full-stack application",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm install",
    "dev": "concurrently \"npm run -w backend dev\" \"npm run -w frontend dev\"",
    "build": "npm run -w backend build && npm run -w frontend build",
    "test": "npm run -w backend test && npm run -w frontend test",
    "lint": "npm run -w backend lint && npm run -w frontend lint",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "prepare": "husky install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "eslint-config-prettier": "^10.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,css,md}": "prettier --write"
  }
}
