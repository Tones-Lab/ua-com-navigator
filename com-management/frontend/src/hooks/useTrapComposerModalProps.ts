import { useMemo } from 'react';

type UseTrapComposerModalPropsArgs = any;

export default function useTrapComposerModalProps(args: UseTrapComposerModalPropsArgs) {
  const {
    open,
    trapModalRef,
    bulkTrapContext,
    trapSource,
    trapSending,
    bulkTrapSummary,
    bulkTrapProgress,
    bulkTrapFailures,
    bulkTrapShowAllFailures,
    setBulkTrapShowAllFailures,
    retryFailedTraps,
    trapServerError,
    trapServerList,
    trapHost,
    setTrapHost,
    trapManualOpen,
    setTrapManualOpen,
    trapPort,
    setTrapPort,
    recentTargets,
    trapVersion,
    setTrapVersion,
    trapCommunity,
    setTrapCommunity,
    trapOid,
    setTrapOid,
    trapVarbinds,
    setTrapVarbinds,
    trapError,
    triggerValidationPulse,
    sendBulkTraps,
    sendTrap,
    onClose,
  } = args;

  return useMemo(
    () => ({
      open,
      trapModalRef,
      bulkTrapContext,
      trapSource,
      trapSending,
      bulkTrapSummary,
      bulkTrapProgress,
      bulkTrapFailures,
      bulkTrapShowAllFailures,
      setBulkTrapShowAllFailures,
      retryFailedTraps,
      trapServerError,
      trapServerList,
      trapHost,
      setTrapHost,
      trapManualOpen,
      setTrapManualOpen,
      trapPort,
      setTrapPort,
      recentTargets,
      trapVersion,
      setTrapVersion,
      trapCommunity,
      setTrapCommunity,
      trapOid,
      setTrapOid,
      trapVarbinds,
      setTrapVarbinds,
      trapError,
      triggerValidationPulse,
      sendBulkTraps,
      sendTrap,
      onClose,
    }),
    [
      open,
      trapModalRef,
      bulkTrapContext,
      trapSource,
      trapSending,
      bulkTrapSummary,
      bulkTrapProgress,
      bulkTrapFailures,
      bulkTrapShowAllFailures,
      setBulkTrapShowAllFailures,
      retryFailedTraps,
      trapServerError,
      trapServerList,
      trapHost,
      setTrapHost,
      trapManualOpen,
      setTrapManualOpen,
      trapPort,
      setTrapPort,
      recentTargets,
      trapVersion,
      setTrapVersion,
      trapCommunity,
      setTrapCommunity,
      trapOid,
      setTrapOid,
      trapVarbinds,
      setTrapVarbinds,
      trapError,
      triggerValidationPulse,
      sendBulkTraps,
      sendTrap,
      onClose,
    ],
  );
}