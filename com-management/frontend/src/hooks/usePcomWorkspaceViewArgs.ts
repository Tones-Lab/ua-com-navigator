import { useMemo } from 'react';

type UsePcomWorkspaceViewArgsArgs = any;

export default function usePcomWorkspaceViewArgs(args: UsePcomWorkspaceViewArgsArgs) {
  const {
    comBrowserPanelProps,
    selectedFile,
    formatDisplayPath,
    browseNode,
    isFavorite,
    toggleFavorite,
    viewMode,
    setViewMode,
    pcomParsed,
    pcomObjectEntries,
    pcomSelectedObject,
    setPcomSelectedObjectKey,
    formatPcomValue,
    searchHighlightActive,
    highlightQuery,
    rawMatchPositions,
    rawMatchIndex,
    handlePrevRawMatch,
    handleNextRawMatch,
    editorText,
    renderRawHighlightedText,
  } = args;

  return useMemo(
    () => ({
      comBrowserPanelProps,
      selectedFile,
      formatDisplayPath,
      browseNode,
      isFavorite,
      toggleFavorite,
      viewMode,
      setViewMode,
      pcomParsed,
      pcomObjectEntries,
      pcomSelectedObject,
      setPcomSelectedObjectKey,
      formatPcomValue,
      searchHighlightActive,
      highlightQuery,
      rawMatchPositions,
      rawMatchIndex,
      handlePrevRawMatch,
      handleNextRawMatch,
      editorText,
      renderRawHighlightedText,
    }),
    [
      comBrowserPanelProps,
      selectedFile,
      formatDisplayPath,
      browseNode,
      isFavorite,
      toggleFavorite,
      viewMode,
      setViewMode,
      pcomParsed,
      pcomObjectEntries,
      pcomSelectedObject,
      setPcomSelectedObjectKey,
      formatPcomValue,
      searchHighlightActive,
      highlightQuery,
      rawMatchPositions,
      rawMatchIndex,
      handlePrevRawMatch,
      handleNextRawMatch,
      editorText,
      renderRawHighlightedText,
    ],
  );
}