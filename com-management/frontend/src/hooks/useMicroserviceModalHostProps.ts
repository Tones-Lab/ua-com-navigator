import { useMemo } from 'react';

type UseMicroserviceModalHostPropsArgs = any;

export default function useMicroserviceModalHostProps(args: UseMicroserviceModalHostPropsArgs) {
  const {
    redeployModalOpen,
    redeployReady,
    showMicroserviceWarning,
    missingMicroservices,
    unhealthyMicroservices,
    microserviceStatusError,
    microserviceIsStale,
    microserviceLastRefreshed,
    redeployError,
    microserviceStatusLoading,
    requiredMicroservices,
    microserviceActionLabel,
    redeployLoading,
    hasEditPermission,
    formatTime,
    getServiceTone,
    getServiceStatusText,
    handleDeployMicroservice,
    handleRedeployMicroservice,
    setRedeployModalOpen,
    setRedeployError,
    setMicroserviceActionLabel,
    refreshMicroserviceStatus,
    handleRedeployFcomProcessor,
  } = args;

  return useMemo(
    () => ({
      redeployModalOpen,
      redeployReady,
      showMicroserviceWarning,
      missingMicroservices,
      unhealthyMicroservices,
      microserviceStatusError,
      microserviceIsStale,
      microserviceLastRefreshed,
      redeployError,
      microserviceStatusLoading,
      requiredMicroservices,
      microserviceActionLabel,
      redeployLoading,
      hasEditPermission,
      formatTime,
      getServiceTone,
      getServiceStatusText,
      handleDeployMicroservice,
      handleRedeployMicroservice,
      setRedeployModalOpen,
      setRedeployError,
      setMicroserviceActionLabel,
      refreshMicroserviceStatus,
      handleRedeployFcomProcessor,
    }),
    [
      redeployModalOpen,
      redeployReady,
      showMicroserviceWarning,
      missingMicroservices,
      unhealthyMicroservices,
      microserviceStatusError,
      microserviceIsStale,
      microserviceLastRefreshed,
      redeployError,
      microserviceStatusLoading,
      requiredMicroservices,
      microserviceActionLabel,
      redeployLoading,
      hasEditPermission,
      formatTime,
      getServiceTone,
      getServiceStatusText,
      handleDeployMicroservice,
      handleRedeployMicroservice,
      setRedeployModalOpen,
      setRedeployError,
      setMicroserviceActionLabel,
      refreshMicroserviceStatus,
      handleRedeployFcomProcessor,
    ],
  );
}